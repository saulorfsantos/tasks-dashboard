import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Required for OpenNext/Cloudflare Pages deployment
  // Do NOT use output: 'export' — we need server-side API routes
}

export default nextConfig
