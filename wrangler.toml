name = "tasks-dashboard"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

# Required for Cloudflare Pages to recognize this as a valid Pages config
pages_build_output_dir = ".open-next"
